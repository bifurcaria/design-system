# UI Package Bundling with tsup

**Date:** 2025-12-04
**Goal:** Configure `packages/ui` to use `tsup` for bundling.

## Prompt
Add tsup to devDependencies and create a `tsup.config.ts` to bundle the library (format: ['cjs', 'esm'], dts: true).

